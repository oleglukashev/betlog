'use strict';


// Declare app level module which depends on filters, and services
var betlog = angular.module('betlog', ['betlog.filters', 'betlog.services', 'betlog.directives', 'betlog.controllers']);
